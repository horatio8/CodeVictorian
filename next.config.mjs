/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/CodeVictorian",
  images: {
    unoptimized: true,
  },
}

export default nextConfig
